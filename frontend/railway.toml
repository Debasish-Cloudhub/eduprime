[build]
builder = "DOCKERFILE"
buildArgs = { NEXT_PUBLIC_API_URL = "https://iscc-backend-production.up.railway.app/api/v1", BUILD_ID = "20260506065227" }

[deploy]
startCommand = "node server.js"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
